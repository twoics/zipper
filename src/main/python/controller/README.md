Here you will find two modules, the `controller` to provide the connection
between the application logic and the UI. 
The `json_connector` is contained and used in the `controller` 
to get data from the JSON file